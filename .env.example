# Configuración de la Base de Datos (Prisma + PostgreSQL)
# Reemplaza 'usuario', 'password', 'localhost', 'port' y 'dbname' con tus valores reales.
DATABASE_URL="postgresql://usuario:password@localhost:port/dbname?schema=public"
# Puerto del servidor backend (según documentación README)
PORT=4000
